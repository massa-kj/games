import { fadeVariants } from "./fade";
import { scaleVariants } from "./scale";
import { slideUpVariants } from "./slide";
import { modalVariants, modalBackdropVariants } from "./modal";

export const variants = {
  fade: fadeVariants,
  scale: scaleVariants,
  slideUp: slideUpVariants,
  modal: modalVariants,
  modalBackdrop: modalBackdropVariants
};
