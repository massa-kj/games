import React from 'react';
import GameContainer from './GameContainer';

function Settings() {
    return (
        <GameContainer>
            <h1>Settings</h1>
        </GameContainer>
    );
}

export default Settings;
